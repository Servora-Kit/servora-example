# Shared TypeScript types (e.g. pagination) for frontends. Output: api/gen/ts
version: v2

clean: true

managed:
    enabled: false

inputs:
    - module: buf.build/servora/servora
      paths:
          - servora/pagination/v1

plugins:
    - local: protoc-gen-typescript-http
      out: api/gen/ts
