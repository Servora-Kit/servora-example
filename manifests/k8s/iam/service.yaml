apiVersion: v1
kind: Service
metadata:
  name: iam
  labels:
    app: iam
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
    - name: grpc
      port: 8001
      targetPort: grpc
      protocol: TCP
  selector:
    app: iam
