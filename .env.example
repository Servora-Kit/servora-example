# gRPC service registration host override (maps to server.grpc.registry.host).
# Use a host IP reachable by both local processes and Docker containers.
# Example:
# G_REGISTRY_HOST=192.168.31.71
G_REGISTRY_HOST=
